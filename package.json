{
  "dependencies": {
    "concurrently": "^7.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev": "concurrently \"cd api && npm run start\" \"cd client && npm run start\" \"cd admin && npm run start\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
